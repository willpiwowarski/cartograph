# Cartograph

A static-analysis tool that builds a type-resolved call graph of a TypeScript
codebase, so you can ask structural questions like "what calls this?" and
"how does a request flow through the system?" — grounded in the real AST and
type checker, not text matching.

Status: in development.